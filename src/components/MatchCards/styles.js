import { StyleSheet } from "react-native";

export default styles = StyleSheet.create({
    cardImage: {
        height: 100,
        width: 100
    },
    container: {
        flex: 1,
        flexDirection: "row",
        justifyContent: "center",
        alignContent: "center"
    }
})