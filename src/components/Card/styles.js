import { StyleSheet } from "react-native"

export default styles = StyleSheet.create({
    cardImage: {
        height: 100,
        width: 100,
        resizeMode: "stretch"
    },
    cardText: {
        fontWeight: "bold",
        fontSize: 24
    },
})